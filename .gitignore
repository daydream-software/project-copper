node_modules
dist
*.local
.DS_Store

# Claude Code per-user settings — keep local, never commit
.claude/settings.local.json

# Playwright MCP scratch output (screenshots/snapshots from verification runs)
.playwright-mcp/
